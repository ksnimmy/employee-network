# Employee Network
This Employee Network app indicates employee’s relationship with other employees.

## Features

1. Fetching employee network data from a JSON file.
2. Create a reusable component to show the employee network(used the same component with different set of data in Page 2)
3. Showing a tooltip on hover of each employee

## How to Run

- `npm install`
- `gulp`